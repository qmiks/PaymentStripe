services:
  api:
    build: .
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
      - STRIPE_DEBUG=1
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --log-level debug
